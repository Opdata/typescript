{
  "compilerOptions": {
    "allowJs": true,
    "target": "ES5",
    "outDir": "./dist", // TS 파일 경로
    "moduleResolution": "Node",
    "lib": ["ES2015", "DOM", "DOM.Iterable"], // Promise, DOM 관련 설정
    "noImplicitAny": true,
    "typeRoots": ["./node_modules/@types","./types"] // 타입스크립트를 지원하는 라이브러리, 개별 라이브러리 로드 설정
  },
  "include": ["./src/**/*"] // 어떤 폴더를 대상으로 타입스크립트를 컴파일 할 것인지
}
